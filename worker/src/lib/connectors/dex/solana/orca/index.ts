const getPools = async (address: string) => {
    return address;
}

const getPoolById = async (address: string) => {
    return address;
}

const getUserPositions = async (address: string) => {
    return address;
}

const addLiquidity = async (address: string) => {
    return address;
}

const removeLiquidity = async (address: string) => {
    return address;
}


export default {
    getPools,
    getPoolById,
    getUserPositions,
    addLiquidity,
    removeLiquidity,
} as const;
